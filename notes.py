# helpful commands

dir(__builtins__) # this will list builin functions you can use, you can then use dir($builtinfunction) to find the methods
help() # python equivelent to man page, useful to find builtin mutables of a builtin function, ex. help(list) help(dict)

# useful commands
isinstance # will take input and pull out what you want
